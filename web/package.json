{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "wasm-pack build ../rust/crates/safemesh-wasm --target bundler --out-dir pkg --release && vite",
    "prebuild": "wasm-pack build ../rust/crates/safemesh-wasm --target bundler --out-dir pkg --release",
    "build": "tsc -b && vite build",
    "lint": "oxlint src",
    "preview": "vite preview",
    "pretest": "wasm-pack build ../rust/crates/safemesh-wasm --target bundler --out-dir pkg --release",
    "test": "vitest run"
  },
  "dependencies": {
    "lucide-react": "^1.23.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.9"
  }
}
