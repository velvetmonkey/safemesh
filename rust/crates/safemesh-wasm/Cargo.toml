[package]
name = "safemesh-wasm"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "WASM/TypeScript binding for the SafeMesh Rust core"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
safemesh-crdt = { path = "../safemesh-crdt" }
wasm-bindgen = "0.2"
