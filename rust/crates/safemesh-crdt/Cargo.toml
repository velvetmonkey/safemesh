# SafeMesh — delta-state CRDT convergence, built on crdt-lean.
# Copyright (C) 2026 Ben Cassie
# SPDX-License-Identifier: Apache-2.0
[package]
name = "safemesh-crdt"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
license = "Apache-2.0"
description = "no_std delta-state CRDT core, held to the SafeMesh Lean proofs by differential conformance"
repository = "https://github.com/velvetmonkey/safemesh"
readme = "README.md"
keywords = ["crdt", "event-log", "no-std", "lean", "mesh"]
categories = ["data-structures", "embedded", "no-std"]

[features]
default = []
local-writer = []
laws = ["local-writer"]

[dependencies]

[dev-dependencies]
serde_json = "1"
